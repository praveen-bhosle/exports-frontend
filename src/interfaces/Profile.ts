export interface Profile {
    firstName?: string,
    lastName?: string,
    email?: string,
    phone?: string,
    country?: string,
    pin?: string,
    address?: string,
    city?: string,
    state?: string
}